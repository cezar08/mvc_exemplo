<h1>Pessoas</h1>

<table>
    <thead>
        <tr>
            <th>Nome</th>
        </tr>
    </thead>

    <tbody>
        <?php foreach ($users as $user) { ?>
        <tr>
            <td><?= $user->name; ?></td>
        </tr>
        <?php } ?>
    </tbody>
</table>

<?= $this->paginationControl($users, 'Sliding',
    'application/partial/paginator',
    ['route' => 'pessoas_index']) ?>